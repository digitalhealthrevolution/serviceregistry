service.url = http://localhost:8080/${project.build.finalName}
auth.secret = ${auth.secret}
db.ip = ${db.ip}
db.port = ${db.port}
db.name = ${db.name}
db.user = ${db.user}
db.pwd = ${db.pwd}
as.ip = ${as.ip}
as.port = ${as.port}

